# QR Test Reference Files

Generated by `generate_test_references.py` using the Python `qrcode` library.

## Regeneration

```bash
pip install qrcode
python generate_test_references.py
```

## Test Cases (74 total)

See `manifest.json` for the full machine-readable list.

## File Format

Each `.txt` file contains the QR module matrix (no quiet zone).
One row per line; each character is `0` (white) or `1` (black).
Parse in APL: `ref ← '1'=↑⊃⎕NGET filename 1`
